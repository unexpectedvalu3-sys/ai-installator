# KinéCotation — dépendances Python
# Testé avec Python 3.12.10 (C:\Users\admin\AppData\Local\Programs\Python\Python312)
# Install : python -m pip install -r requirements.txt

mistralai==2.7.0      # provider IA par DÉFAUT (éditeur FR, serveurs UE) — prototype/llm.py
anthropic==0.117.0    # provider alternatif / référence de qualité au benchmark
pydantic==2.13.4      # structured output de l'OCR
pillow==12.3.0        # manipulation images ordonnances — benchmark/, prototype/
flask==3.1.3          # webapp/server.py
itsdangerous==2.2.0   # cookie de session signe — webapp/auth.py (dep de Flask, epinglee)
python-dotenv==1.2.2  # charge webapp/.env — webapp/server.py

# Dépendance système (hors pip) : poppler — pour lire/rasteriser les PDF NGAP du SNMKR.
#   winget install --id oschwartz10612.Poppler --exact
#   Fournit pdftoppm / pdftotext. Installé le 2026-07-17.
